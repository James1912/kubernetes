apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: couchdb001-data-volume-claim
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
      storage-iops: 2000
